{
  "name": "redis-gcra",
  "version": "0.1.0",
  "main": "lib/index.js",
  "author": "Losant <hello@losant.com>",
  "license": "MIT",
  "description": "Rate limiting based on Generic Cell Rate Algorithm",
  "engines": {
    "node": ">=8.3"
  },
  "scripts": {
    "lint": "esw *.js lib test",
    "lint:fix": "esw --fix *.js lib test",
    "lint:watch": "esw --watch *.js lib test",
    "lint:staged": "lint-staged",
    "test": "mocha --expose-gc test 2>&1",
    "test:watch": "mocha --expose-gc --watch test 2>&1",
    "reinstall": "rm -rf node_modules && yarn install",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.js": "esw"
  },
  "dependencies": {
    "ioredis": ">=2.0.0"
  },
  "devDependencies": {
    "@losant/eslint-config-losant": "^1.2.0",
    "husky": "^0.14.3",
    "lint-staged": "^4.3.0",
    "mocha": "^4.0.1",
    "should": "^13.1.2"
  },
  "eslintConfig": {
    "extends": "@losant/eslint-config-losant/env/node"
  }
}
